PORT=4000

# Set to false to use PostgreSQL instead of mock data
USE_MOCK_DATA=false

# PostgreSQL connection
DB_HOST=localhost
DB_PORT=5432
DB_NAME=ethereum_airdrop
DB_USER=postgres
DB_PASSWORD=your_password

# Allowed origin for CORS (Next.js frontend)
CORS_ORIGIN=http://localhost:3000
